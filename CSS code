.background {
  background-image: url("best.jpg");
  background-color: silver;
  background-size: contain;
  background-position: fill;
  min-height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
}

.container {
  width: 80%;
  margin: 0 auto;
  color: #ccd0d4;
}

header {
  text-align: center;
  margin: 10px 0; /* Decreased margin */
}

header h1 {
  margin: 20px 0; /* Decreased margin */
}

.section {
  padding: 40px; /* Decreased padding */
  text-align: center;
}

.rectangle {
  display: flex;
  justify-content: space-around; /* Align buttons evenly */
  align-items: center; /* Center buttons vertically */
  border-radius: 30px;
  background-color: rgba(0, 0, 0, 0.7); /* Semi-transparent black */
  padding: 10px;
  flex-wrap: wrap; /* Allow buttons to wrap if they exceed container width */
}

.projects {
  padding: 40px;
  text-align: center;
}

.project-container {
  display: flex;
  overflow-x: auto; /* Enable horizontal scrolling */
  scroll-behavior: smooth; /* Smooth scrolling */
}

.project-box {
  width: 300px;
  height: 200px;
  background-color: black;
  margin: 10px;
  border-radius: 10px;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-shrink: 0; /* Prevent project boxes from shrinking */
  transition: transform 0.3s ease;
}


.project-box:hover {
  transform: scale(1.1); /* Scale up effect on hover */
}

.project-box h3 {
  margin-bottom: 10px;
}

.project-box p {
  font-size: 14px;
}

/* Additional styling for section heading */
.projects h2 {
  margin-bottom: 20px;
}

/* Animation for sliding effect */
@keyframes slideRight {
  0% {
      transform: translateX(-100%);
  }
  100% {
      transform: translateX(0%);
  }
}

.project-box.animated {
  animation: slideRight 1s forwards;
}


.rectangle button {
  background-color: transparent;
  border: none;
  color: #fff;
  padding: 10px 20px;
  border-radius: 30px;
  cursor: pointer;
  transition: transform 0.3s ease, opacity 0.3s ease; /* Smooth button hover effect */
  margin: 5px; /* Adjusted margin */
}


.rectangle button:hover {
  transform: scale(1.1); /* Scale up effect on button hover */
  opacity: 0.8; /* Reduce opacity on hover */
}

.circle-double {
  object-fit: contain;
  
}

.circle-outline {
  border: 5px solid #fff;
  border-radius: 50%;
  width: 300px;
  height: 300px;
  margin: 10px auto; /* Decreased margin */
  overflow: hidden;
  animation: rotate 3s linear infinite; /* Rotate profile picture */
}

.circle-outline img {
  width: 100%;
  height: 100%;
  object-fit: cover; /* Adjusted to fit the image inside the circle */
  margin: 0px; /* Decreased margin */
  animation: zoomIn 1s ease; /* Zoom in effect */
}


.about-me-text {
  font-size: 16px;
  font-weight: bold;
  font-family: 'Times New Roman', Times, serif;
  margin-bottom: 5px; /* Decreased margin */
  margin-top: 5px; /* Decreased margin */
}

.download-cv, .social-icons {
  margin-top: 10px; /* Decreased margin */
}

.skills-buttons {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
}

.skills-buttons button {
  background-color: rgba(0, 0, 0, 0.7); /* Semi-transparent black */
  border: none;
  color: #fff;
  padding: 10px 20px;
  margin: 5px; /* Adjusted margin */
  border-radius: 20px; /* Curved edges */
  cursor: pointer;
  transition: transform 0.3s ease, opacity 0.3s ease; /* Smooth button hover effect */
}

.skills-buttons button:hover {
  transform: scale(1.1); /* Scale up effect on button hover */
  opacity: 0.8; /* Reduce opacity on hover */
}


@keyframes zoomIn {
  from {
    transform: scale(0);
  }
  to {
    transform: scale(1);
  }
}
